{
  "id": "1be59088",
  "title": "Review opencode-access service against opencode-cloudflare extension",
  "tags": [
    "analysis",
    "pi",
    "opencode-cloudflare"
  ],
  "status": "completed",
  "created_at": "2026-05-19T16:19:08.869Z",
  "assigned_to_session": "019e4107-cdfc-7ea6-9dbc-bb05e68ef3a2"
}

Completed read-only review. Cloned `opencode-access` with `GITLAB_HOST=gitlab.cfdata.org glab repo clone cloudflare/devtools/ai/opencode-access /tmp/opencode-access.f9mNyx/opencode-access` after full-URL clone tried HTTPS credentials. Reviewed worker `src/index.ts`, config build/base, API/architecture docs, deployed `/.well-known/opencode`, and local `agent/extensions/opencode-cloudflare/*`. No implementation changes made.

Main extension recommendation: honor provider `blacklist` values from gateway well-known. The deployed/current service blacklists `gpt-5.4-pro` and `gpt-5.5-pro`, but the extension catalog currently exposes both because `GatewayWellKnownResponse`/catalog ignore blacklists. Filter both built-ins and dynamic `route.models` to prevent re-add.

Also recommended: refresh extension fixtures/fallback Workers AI catalog to current service shape (`cloudflare-workers-ai`, short model keys, Kimi K2.6 + Gemma + GLM 5.1, 32k caps where configured), and add regression coverage for aliases/current blacklist semantics. Optional defense-in-depth: constrain the fixed-provider login auth command rather than accepting arbitrary remote string shell commands.

Service-side observations to present separately: `src/index.ts` overwrites `config.provider.openai.models` at response time, so the hand-authored `gpt-5.2` variants/include options in `config/base.json` are not served; JWT verification visibly checks JWKS/email but not app audience/issuer in worker code (confirm trust model); D1 anonymous-ID lookup errors can escape the structured-error helper.
