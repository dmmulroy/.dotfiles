{
  "id": "03e6f73e",
  "title": "Implement Pi Ephemeral extension from approved plan",
  "tags": [
    "pi",
    "extension",
    "ephemeral"
  ],
  "status": "done",
  "created_at": "2026-04-01T13:40:23.150Z"
}

Implemented Pi Ephemeral as a package-style extension under `agent/extensions/pi-ephemeral/`.

Completed:
- catalog scanning for skills, prompts, extensions, and MCP servers from `~/.pi/ephemeral`
- manifest read/write via `.pi/ephemeral.json`
- project state inspection and drift/conflict warnings
- apply/remove semantics for copied resources, settings references, and MCP server merges
- two-column overlay UI with category tabs, search, toggle staging, preview mode, and apply
- `/ephemeral` command and startup warning check
- package manifest and workspace-local TypeScript config

Validation:
- `npm run typecheck --workspace pi-extension-ephemeral`
