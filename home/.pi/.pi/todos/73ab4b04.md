{
  "id": "73ab4b04",
  "title": "Implement opencode-cloudflare service alignment improvements",
  "tags": [
    "pi",
    "opencode-cloudflare",
    "implementation"
  ],
  "status": "completed",
  "created_at": "2026-05-19T16:29:46.528Z",
  "assigned_to_session": "019e4107-cdfc-7ea6-9dbc-bb05e68ef3a2"
}

Implemented approved opencode-cloudflare alignment work.

Changes:
- Parsed provider `blacklist` fields from well-known configs and filtered blacklisted built-in, dynamic, and Workers AI catalog entries while matching both prefixed and short model IDs.
- Refreshed fallback Workers AI model defaults to current opencode-access shape: short Cloudflare Workers AI keys + full request IDs, Kimi K2.6/Gemma/GLM 5.1 additions, current costs/limits, and 32k output caps where the service specifies them.
- Updated well-known fixture to current `cloudflare-workers-ai` provider alias and current service-like model/blacklist semantics.
- Hardened native login auth command execution: only array commands, only `cloudflared access login`, exactly one trusted `-app`/`--app` origin matching `https://opencode.cloudflare.dev`, no shell string execution.
- Added `test/gateway-config-regression.mjs` for blacklists/current workers routing and `test/auth-command-regression.mjs` for rejected login command shapes; wired both into package `check`.
- Updated README notes.

Validation:
- `cd agent/extensions/opencode-cloudflare && npm run check` ✅
- `npm run check` ✅
- Live well-known catalog probe after change showed `gpt-5.4-pro=false`, `gpt-5.5-pro=false`, Kimi K2.6/GLM 5.1 true, counts `{anthropic:23, openai:48, google:27, workers-ai:6}`.
- `git diff --check` ✅
- `npx tsc --noEmit` was also tried, but this workspace currently has pre-existing broader type errors outside this change (todos/web-tools plus the already-existing `ctx.ui.notify(..., \"success\")` line in opencode-cloudflare/index.ts); none pointed at the files changed here.
