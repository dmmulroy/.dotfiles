# pi-extension-update

Pi extension that adds a `/update` command for updating the installed Pi CLI.

## Install

```sh
pi install ~/.dotfiles/home/.pi/extensions/update
```

## Commands

- `/update`

## Notes

- Detects whether Pi was installed via bun, npm, or Homebrew.
- Local-path installation keeps the extension source in place.
